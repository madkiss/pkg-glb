# Defaults for glb initscript
# sourced by /etc/init.d/glb
# installed at /etc/default/glb by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
LISTEN_ADDRESS="127.0.0.1:3128"
# Disabled by default (also can be configured in runtime)
#DESTINATION_LIST="example.com:80"
